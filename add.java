class add{
    public static void main(String[] args) {
        int a=10;
        int b=20;
        int sum=a+b;
        System.out.println("The sum of a and b is: " + sum);
    }
}
This code defines a class named `add` with a `main` method that initializes two integer variables `a` and `b` with the values 10 and 20, respectively. It then calculates the sum of `a` and `b`, stores it in the variable `sum`, and prints the result to the console.